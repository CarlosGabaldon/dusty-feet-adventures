<h2><%= @trail.name %></h2>

<p><%= image_tag @map_uri, id: "map_#{@trail.id}" %>

<p><%= @trail.description %> </p>

<p><%= @trail.location.geocode %> </p>

<%= link_to "Edit Trail", edit_trail_path(@trail) %>

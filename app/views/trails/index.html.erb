<h1>Trails</h1>
<h2>Trails in your area</h2>

<ul>
  <% @trails.each do |trail| %>
    <li><%= link_to trail.name, trail_path(trail) %></li>
  <% end %>
</ul>

<%= link_to "Add new trail", new_trail_path %>
